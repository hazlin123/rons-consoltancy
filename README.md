# Project

<!-- Deployment trigger: 2026-01-27-1130 -->

## Project info

This repository contains a Vite + React + TypeScript project scaffold.

## How to edit and run locally

Clone the repository, install dependencies and run the dev server:

```sh
git clone <YOUR_GIT_URL>
cd <YOUR_PROJECT_NAME>
npm install
npm run dev
```

## Technologies

- Vite
- TypeScript
- React
- shadcn-ui
- Tailwind CSS

## Deploy

Build and deploy to your provider (Vercel/Netlify) using the standard build command:

```sh
npm run build
```

If you use a CI/CD provider, configure it to run `npm ci`, `npm run build`, and any tests or linting steps you require.

## Notes

- Replace `<YOUR_GIT_URL>` and `<YOUR_PROJECT_NAME>` with your repository details.
- Add an `opengraph.png` file to the `public/` folder if you want social previews.
